# Tenant RBAC Admin

Vue 3 + Vite + TypeScript 多租户 RBAC 后台管理端。

## 运行

```bash
npm.cmd install
npm.cmd run dev -- --port 5173
```

浏览器打开：

```text
http://localhost:5173/login
```

不要直接双击 `index.html` 打开。Vite 项目需要通过开发服务器运行，否则页面模块资源不会按预期加载。
